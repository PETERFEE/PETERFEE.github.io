APB UART Master Core Verification
=================================

Drop the images for this project into THIS folder, using these exact filenames.
The site picks them up automatically — no code change needed.

  tb-architecture.png
      [diagram] UVM env diagram: two APB agents → two UART DUVs → shared scoreboard

  coverage-report.png
      [chart] Coverage report showing 100% functional coverage

  waveform.png
      [waveform] Waveform of an APB write followed by the serial frame on the line

Aim for ~1600px on the long edge. Images are cropped with object-cover to fit the frame.